export default {
    /**
     * @token在Cookie中存储的天数，默认30天
     */
    cookieExpires: 30,
    /**
     * @默认打开的首页的路由name值，默认为home
     */
    homeName: '/',
    /**
     * @默认打开的首页的路由name值，默认为home
     */
    tableHeight: 340
}
